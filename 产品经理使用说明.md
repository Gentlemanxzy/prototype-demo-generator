# 产品经理使用说明

本说明用于帮助产品经理把用户指令、需求、Figma 链接、UI图/原型图和素材交给 Codex，快速生成并持续改进一套可向客户演示的水厂前端页面。

## 推荐提问方式

可以直接复制下面的格式使用。

```text
请使用 prototype-demo-generator skill，根据我提供的需求、Figma/UI图/原型图和素材，生成一套水厂演示系统前端。

要求：
1. 以我当前这段文字指令为最高优先级。
2. 历史对话中已确认且未作废的规则继续有效。
3. Figma/UI图/原型图负责布局、视觉、模块数量、点位和页面结构。
4. 需求文档主要用于补充操作逻辑、数据要求、指标含义、字段口径和演示重点。
5. 需求文档不要覆盖当前指令或 Figma/UI图中的布局和视觉。
6. figma-links.txt 中的所有有效链接都要生成；同一模块下多个链接请识别为弹窗、展开态、子页面或切换态。
7. 没有需求文档对应说明的 Figma 页面也要按 UI 图生成，缺少真实数据时可使用合理演示数据，不需要在界面明确标注模拟数据。
8. UI图中没有承载区域的需求不要自由发挥，请登记为未映射需求。
9. 如果样式规范、页面模板和UI图冲突，以UI图为准。
10. 先输出分析文件，再生成页面代码。

项目名称：
客户类型：
水厂类型：
演示目标：
页面范围：
重点模块：
视觉要求：
是否需要登录页：否
是否需要多页面导航：
是否需要图表：
是否需要模拟数据：

附件：
1. 需求文档
2. 当前文字指令或修改意见
3. Figma 链接、figma-links.txt、.fig 文件、UI图/原型图或截图
4. 背景图、logo、图标、地图、厂区图等素材
```

## 最简输入

如果时间很紧，也可以只写：

```text
请使用 prototype-demo-generator skill，根据这些 Figma/UI图/原型图、素材和我的文字指令生成一套可预览的水厂前端演示系统。当前文字指令优先；Figma/UI图负责布局和视觉；需求文档主要看操作逻辑和数据要求；不要自由新增UI图里没有的模块；没有真实数据时可以使用合理演示数据，不需要在界面明确标注模拟数据。
```

## 多轮改进输入

生成后可以继续直接提修改意见，例如：

```text
继续基于上次生成的页面修改：
1. 顶部导航样式按 A1 UI图重新贴近。
2. 运行监测的图表补上横纵坐标和指标切换。
3. 删除左侧主视角、空间剖切、测量这些工具栏。
4. 其他页面先不要动。
```

Agent 应把本轮修改意见记录到 `analysis/user-instructions.md`，只改受影响页面和组件。

## Figma 链接清单格式

如果有多个 Figma 页面，可以提供 `figma-links.txt`，推荐格式：

```text
A1-场景总览:https://www.figma.com/design/xxx?node-id=25429-7480&m=dev
A2-运行监测:https://www.figma.com/design/xxx?node-id=25429-7658&m=dev
```

Agent 应读取清单中的所有链接，建立页面名称、node-id 和页面功能的对应关系，不要只处理第一条链接。没有需求文档对应说明的链接也必须生成；同一模块下多个链接通常表示弹窗、展开态、详情页、子页面或切换态。

## 建议提供的信息

为了生成效果更稳定，建议尽量提供：

- 项目名称
- 当前文字指令或修改意见
- 需要展示的页面
- Figma 链接或 `figma-links.txt`
- UI图/原型图或截图
- 背景图、logo、图标、地图、厂区图等独立素材
- 必须出现的业务指标
- 客户最关心的演示重点
- 是否偏大屏

## 常见演示重点

- 运行态势一屏总览
- 水处理工艺流程可视化
- 水质实时监测
- 设备运行监控
- 告警闭环处置
- 能耗统计分析
- 领导驾驶舱展示

## 交付结果

Agent 应交付：

- 可本地预览的前端页面
- UI图和需求的映射说明
- 未映射需求清单
- 主要页面导航
- 中文业务文案
- 水厂行业演示数据
- 图表和状态面板
- 预览地址
- 简短的生成说明和剩余差异说明
